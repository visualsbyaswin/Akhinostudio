import React from "react";
import "./button.css"
const Button = ({content}) => {
  return (
   <>
    <button> 
      <p> {content}</p> 
      </button>
   </>
     
    
  );
};

export default Button;
